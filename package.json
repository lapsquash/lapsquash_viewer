{
  "name": "lapsquash",
  "description": "lapsquash",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "yarn workspace viewer run build",
    "dev": "yarn workspace viewer run dev",
    "dev:analyzer": "yarn workspace analyzer run dev"
  },
  "workspaces": {
    "packages": [
      "analyzer",
      "viewer"
    ]
  }
}
